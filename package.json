{
  "name": "formulaire-db",
  "version": "1.0.0",
  "description": "Formulaire simple avec envoi des données vers une base SQLite",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@libsql/client": "^0.17.4",
    "express": "^4.19.2"
  }
}
